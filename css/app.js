console.log('app.js is connected');


//ENEMY APPEARS
    enemy array = []

//SELECT TEAM MEMBER

        getAttacker();
        getDefender();
        initiateBattle();
        prepareBattleField();
        displayMoves();
        displayHealth();
    });

    function getAttacker() {
        attacker = choose from fighter array
    }

    function getDefender() {
      opponent = random from array (will loop in order);
    }

    function prepareBattleField() {
      use jquery to access DOM
    }

    function displayMoves() {
      use jquery to access DOM
    }

    function displayHealth() {
      use jquery to access DOM
    }

//ELEMENT TYPE

//FIRE
      if(attacker type = 'Fire' && defender type = 'Ice'){
        attacker dmg= attacker dmg stat / 2;

      } else if(attacker typ = 'Fire' && defender type = 'Lightnin'){
      } else if(attacker type = 'Fire' && defender type = 'Wind'){

//ICE

//LIGHTNING

//WIND


//NEUTRAL TYPE
        else if(attacker type = defender type){


  },


//FIRST TO ATK DISPLAY
      if(attacker speed > defender speed){
        console.log('You attack first')
      } else {
        console.log('Enemy attacks first')
      }

//EVADE OR DEFEND
      var evadeMove = function(){
      if(evade
      } else if defend


//BATTLE DISPLAY LOG


//ENEMY ATTACKS BACK

        var defenderAttack
        var defenderAttackName
        var randomMove
        if
          defenderAttack
          defenderAttackName
        } else if
          defenderAttack
          defenderAttackName
        } else if
          defenderAttack
          defenderAttackName
        }

        Log Player Stats




//END/RESTART BATTLE
